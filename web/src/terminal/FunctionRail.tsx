import {NavLink} from 'react-router-dom';import {Search,Columns3,ScanLine,Bookmark,Bell,Settings} from 'lucide-react';import {useWorkspace} from '../workspace/store';
export function FunctionRail(){const s=useWorkspace();const items=[['Search','/search',Search],['Compare',s.selected[s.group]?'/compare/'+s.selected[s.group]:'/search',Columns3],['Scanner','/scanner',ScanLine],['Watchlists','/watchlists/main',Bookmark],['Alerts','/alerts',Bell],['Settings','/settings',Settings]] as const;return <nav className="function-rail" aria-label="Functions">{items.map(([name,url,Icon])=><NavLink key={name} to={url} title={name} aria-label={name}><Icon size={19}/><span>{name.slice(0,4).toUpperCase()}</span></NavLink>)}</nav>}
