import type {Comparison} from '../../services/api';
export function RulesDiff({data}:{data:Comparison}){return <div><div className="label">DETERMINISTIC DIFFERENCES</div><ul className="differences">{data.match.differences.map((d,i)=><li key={i}>{d}</li>)}</ul><div className="two-rules">{data.markets.map(m=><details key={m.id} open><summary className={m.venue==='kalshi'?'cyan':'violet'}>{m.venue.toUpperCase()} / FULL RULE TEXT</summary><a href={m.source_url} target="_blank" rel="noreferrer">View original contract ↗</a><p className="rule-text">{m.settlement.rule_text}</p><p className="hash">SHA256 {m.settlement.rule_hash}</p><p>Close: {m.close_time?new Date(m.close_time).toISOString():'Unknown'}</p><p>Payout: {m.settlement.payout} {m.settlement.currency}</p></details>)}</div></div>}
