import type {Comparison} from '../../services/api';import {RulesDiff} from './RulesDiff';import {ReviewControls} from './ReviewControls';
export function RulesPanel({data}:{data:Comparison}){return <><div className="match-state"><span className="tag amber">{data.match.classification}</span><span className="tag">{data.match.review_state}</span><b>{Math.round(data.match.score*100)}<small> MATCH SCORE</small></b></div><p className="muted">Score measures candidate similarity, not equivalence or profit probability.</p>{data.match.review_reason&&<p className="notice">{data.match.review_reason}</p>}<RulesDiff data={data}/><ReviewControls match={data.match}/></>}
