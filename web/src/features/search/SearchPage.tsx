import {useSearchParams} from 'react-router-dom';import {SearchResultsPanel} from './SearchResultsPanel';
export function SearchPage(){const [params]=useSearchParams();return <div className="search-page"><SearchResultsPanel key={params.get('q')} initialQuery={params.get('q')??''}/><div className="welcome"><div className="eyebrow">PREDICTION MARKET RESEARCH / 01</div><h1>One workspace.<br/>Two market perspectives.</h1><p>Compare contracts by what they pay, not just what they’re called.</p><div className="workflow"><div><b>01</b><h3>Discover</h3><p>Search Kalshi and Polymarket, or import an event URL.</p></div><div><b>02</b><h3>Review</h3><p>Read resolution terms, outcome boundaries and dates side by side.</p></div><div><b>03</b><h3>Simulate</h3><p>Consume quoted depth and inspect cash, scenarios and eligibility.</p></div></div><div className="command-guide"><code>SRCH</code> Find an event <code>COMP</code> Open a match <code>SCAN</code> Review candidates <code>⌘ K</code> Focus commands</div><p className="footnote">Local, read-only research. Unreviewed rules, unknown fees and stale data cannot produce a verified opportunity.</p></div></div>}
