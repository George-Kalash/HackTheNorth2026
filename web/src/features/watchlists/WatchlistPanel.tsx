import {Link} from 'react-router-dom';import {useMutation,useQueryClient} from '@tanstack/react-query';import {api,type Watchlist,type ScannerRow} from '../../services/api';
export function WatchlistPanel({list,rows}:{list:Watchlist;rows:ScannerRow[]}){const cache=useQueryClient(),remove=useMutation({mutationFn:(id:string)=>api('/watchlists/'+list.id,{name:list.name,match_ids:list.match_ids.filter(m=>m!==id)},'PUT'),onSuccess:()=>cache.invalidateQueries({queryKey:['watchlists']})});return <section className="panel"><div className="panel-title">{list.name.toUpperCase()}<span className="tabs-fill"/>{list.match_ids.length} contracts</div><div className="panel-body"><table><thead><tr><th>Comparison</th><th>Review</th><th>Action</th></tr></thead><tbody>{list.match_ids.map(id=>{const row=rows.find(r=>r.match.id===id);return <tr key={id}><td><Link to={'/compare/'+id}>{row?.match.title??id}</Link></td><td>{row?.match.review_state??'Unknown'}</td><td><button onClick={()=>remove.mutate(id)}>Remove</button></td></tr>;})}</tbody></table>{!list.match_ids.length&&<p className="empty compact">Add a comparison using its + Watchlist button.</p>}{remove.error&&<p className="error">{remove.error.message}</p>}</div></section>}
