import type {ReactNode} from 'react';import {Maximize2,Minimize2,ChevronDown,ChevronRight} from 'lucide-react';import {useWorkspace} from './store';
export function PanelFrame({id,title,children,tools}:{id:string;title:string;children:ReactNode;tools?:ReactNode}){const s=useWorkspace(),collapsed=s.collapsed.includes(id);if(s.maximized&&s.maximized!==id)return null;return <section className={'panel '+(s.maximized===id?'maximized':'')} data-panel={id}><div className="panel-title"><button aria-label={`${collapsed?'Expand':'Collapse'} ${title}`} onClick={()=>s.set({collapsed:collapsed?s.collapsed.filter(p=>p!==id):[...s.collapsed,id]})}>{collapsed?<ChevronRight size={13}/>:<ChevronDown size={13}/>}</button><span>{title}</span><div className="panel-tools">{tools}<button aria-label={`Maximize ${title}`} onClick={()=>s.set({maximized:s.maximized===id?null:id})}>{s.maximized===id?<Minimize2 size={13}/>:<Maximize2 size={13}/>}</button></div></div>{!collapsed&&<div className="panel-body">{children}</div>}</section>}
