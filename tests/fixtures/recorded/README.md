# Recorded public adapter contracts

These small responses were retrieved on 2026-09-19 from the exact source URLs/parameters embedded in each JSON envelope. Each includes retrieval time and a SHA-256 identifier. They contain public contract metadata or books, no authentication headers or user/account data. They are offline schema/normalization regression inputs, not current trading quotes. Refresh explicitly with `scripts/record_fixtures.py` after an opt-in live smoke run and review the diff.
